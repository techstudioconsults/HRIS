# ============================================================================
# Dashboard App URL
# The base URL of the user-dashboard application — used to construct
# cross-app links (Login, Register, Start Free Trial) from the landing page.
#
# Local development:  http://localhost:3000
# Production:          https://dashboard.techstudiohr.com
# ============================================================================
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000

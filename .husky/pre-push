git pull origin main

# shellcheck disable=SC2102
pnpm turbo run typecheck --filter=[origin/main...HEAD] || {
  echo "Type checking failed — run 'pnpm typecheck' locally to see full output"
  exit 1
}

# shellcheck disable=SC2102
pnpm turbo run build --filter=[origin/main...HEAD] || {
  echo "Build failed — run 'pnpm build' locally to see full output"
  exit 1
}

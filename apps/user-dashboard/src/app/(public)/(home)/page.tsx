"use client";

// import { useSession } from "next-auth/react";

const Page = () => {
  return (
    <section className={`flex h-[100dvh] w-full items-center justify-center`}>
      <p className={`text-primary-500 text-xl`}>TECHSTUDIO HR</p>
    </section>
  );
};
export default Page;
